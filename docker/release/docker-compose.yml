services:
  nginx:
    tty: true
    image: nginx:1.29.0
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    ports:
      - 80:80

  backend:
    tty: true
    build:
      context: ../..
      dockerfile: ./docker/release/Dockerfile
